namespace Malumware.BetaTeam.Lib.IO.Fin.Blocks
{
    // Whether a node sorts its children (e.g. for transparency) before drawing them
    public enum NiSortingMode : uint
    {
        On = 0,
        Off = 1,
        Default = 2,
    }
}
